# Experiment Tree Requirements
# Install with:
#   pip install -r design_experiment/requirements_experiment_tree.txt

# Core
python-dotenv>=0.19.0
openai>=1.0.0
numpy>=1.20.0

# LangGraph (used by the literature search workflow)
langgraph>=0.0.40

# Search + ArXiv utilities
requests>=2.25.0
feedparser>=6.0.0
PyPDF2>=3.0.0

# FAISS semantic retrieval (recommended, else ArXiv-only path still works)
faiss-cpu>=1.7.0

# Embeddings (recommended for better relevance filtering)
sentence-transformers>=2.2.2
torch>=2.0.0

# Tree search algorithm
treequest
